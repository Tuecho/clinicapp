services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "5173:5173"
    env_file:
      - .env
    networks:
      - proxy-network
    restart: unless-stopped

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: api
    ports:
      - "3000:3000"
    volumes:
      # Mapeamos la carpeta completa para que los cambios en el código se vean al instante y use la BD local
      - ./backend:/app
      # Volumen para persistencia de otros datos si fuera necesario
      - api-db:/app/database 
    env_file:
      - .env
    networks:
      - proxy-network
    restart: unless-stopped

  nginx-proxy-manager:
    image: jc21/nginx-proxy-manager:latest
    container_name: nginx-proxy-manager
    ports:
      - "80:80"
      - "443:443"
      - "81:81"
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Madrid
    volumes:
      - ./npm/data:/data
      - ./npm/letsencrypt:/etc/letsencrypt
    networks:
      - proxy-network
    restart: unless-stopped

volumes:
  api-db:
  npm-data:
  npm-letsencrypt:

networks:
  proxy-network:
    name: proxy-network
    external: false